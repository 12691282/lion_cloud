server :
  port : 0
#logging : 
#  path : config_log_${random.int}
spring : 
   application : 
     name : config-service
   cloud :
     config :
       server :
         native :
           searchLocations : file:////config_file
   profiles :
     active : native
     rabbitmq :
      host : localhost
      port : 5672
      username : root
      password : root   
eureka : 
  instance : 
    instance-id : ${spring.application.name}:${random.int}
  client : 
    serviceUrl : 
      defaultZone : http://localhost:2001/eureka

